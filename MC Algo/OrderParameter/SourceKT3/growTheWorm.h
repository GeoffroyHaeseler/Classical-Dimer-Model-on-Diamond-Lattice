/// <summary>
/// Choose the next tetrahedron. If it is a worm piece, return false : we need to close the loop. If not, return true
/// </summary>
/// <param name="Worm1s"></param>
/// <returns></returns>
void growTheWorm(int* x, int* y, int* z, char* tetra, int* n);
